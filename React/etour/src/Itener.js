import React from 'react'

export default function Itener(props) {
    console.log(props.name.destIternary)
    var str = props.name.destIternary
    var result = str.split("*") 
    return(<>
        {/* {[result].map((t, i) => <div key={i}>{t}</div>)} */}
       <p>fjbhuegboiuhepio9jh</p>
   </> )
   

}